pipeline {
   agent any
   tools {
      maven 'Maven'
   }
   stages {
       stage("s1") {
                steps {
                    snDevOpsStep ()
                    echo "Building" 
                    sh 'mvn -X clean install -DskipTests'
                }
       }
      stage("s2") {
           steps {
               snDevOpsStep ()
               sh 'mvn test'
           }
          post {
                always {
                    junit '**/target/surefire-reports/*.xml' 
                }
          }
        }
    
      stage("s3") {
             steps{
                  snDevOpsStep ()
                  echo "deploy in progress"       
                   snDevOpsChange()
              }
      }      
      
  }
}
