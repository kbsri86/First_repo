pipeline {
   agent any
   tools {
      maven 'Maven'
   }
   stages {
       stage("Deploy") {
             steps{
                  snDevOpsStep ()
                  snDevOpsChange()
                  echo "deployment started"       
              }
      }
      
  }
}
